# Straumr.Console.Tui

@AGENTS.md

Follow `AGENTS.md` in this directory: read `docs/tui/status.md` first, then only the
documents its routing table names for the task in hand. The TUI specification is split
across `docs/tui/` precisely so it does not have to be read whole.
